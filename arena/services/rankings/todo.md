# Rankings and points remaining work

- Implement the observed Launcher friend-point lookup: `proximitylist` on `ngpsglobal` and `ngps`, `above=below=0`, and a comma-separated `userList` with a trailing comma/space. Establish the returned membership and rank scope from the native consumer before enabling it.
- Implement further boards and periods only when supported; reject unsupported queries.
- Establish an authenticated association for remote native writes that omit cookies.
- Community points have no implemented source and remain zero.
