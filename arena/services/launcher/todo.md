# N-Gage Launcher remaining work

- Fix live invitation and friend-list refresh with the messaging service; rejection and reinvitation after recipient restart are verified.
- Implement the observed friend point queries (`proximitylist`, `ngps` / `ngpsglobal`, nonempty `userList`) and verify the consuming native screen.
- Cover live Store/catalogue service screens and long-lived session recovery; cached Store rendering is not server validation.
- Repeat the input/background/login controls when the host integration changes.
