# N-Gage Launcher remaining work

- Implement live web Rankings content and cover Store/catalogue service screens and long-lived session recovery; cached Store rendering is not server validation.
- Repeat the input/background/login controls when the host integration changes.
