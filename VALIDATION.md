# Native validation, 13 September 2026

## Legacy access points and retail Tomb Raider retest — 14 September 2026

The multilingual High Seize 1.0.2 archive was installed on two iOS simulators with its executable and DLLs unchanged. The reversible `highseize_setup` helper configured the native Arena XML endpoints and host mappings. Both clients selected **Host network**, completed Community and SNAP login, and reached room/commander setup. EKA2L1 exposes both legacy progress stages, EConnectionOpen (14) and EIfProgressLinkUp (1000). Its CommsDB patch creates the IAP and WAP associations through the ROM's database API; repeated opens and an existing configured database were byte-identical in the native probe.

The supplied multilingual Tomb Raider 1.0 archive contains a duplicate `tr` application registration whose executable is truncated to 512 bytes. That duplicate directory was backed up outside the guest. The real `tombraider.app` then launched successfully without executable changes. Its retail GSB launcher already exposes Arena; the existing sent-message cache repair was applied while preserving its import/protection data. Native login, directory browsing, download of a 642-byte Caves recording and playback passed. After removing the test installation's writable CommsDB file with a backup, the final WAP patch recreated it and Arena connected again using the rebuilt billing replacement, which no longer imports or edits CommsDB. A practice race completed, the native taunt form submitted, and the result page displayed **0.701 seconds, 0 points**, matching the persisted 701 ms result and noncompetitive challenge. This is a practice-result retest, not a new two-account challenge validation.

The Tomb Raider practice result used Release executable SHA-256 `7dfdb416b493c973980fda654dbc7a6b1df3b56758a7d5be922cbc2bb2f9c090`. A subsequent legacy redraw correction prevents creation of an incremental drawing segment from clearing previously drawn pixels when redraw storing is disabled. On Release `fed9cd5c20a03cfaaddd8079d37c513d3115407b6d6afe4359d20a9087995c5c`, both High Seize login forms and Arena menus rendered completely. Tomb Raider reauthenticated and retrieved its persisted **0.701-second** Arena score with the full page visible. Its passwordless device identity was explicitly linked to the shared `adgjmptw` account using the authenticated account-binding API.

The final High Seize check ran through `arena.runtime` with two native clients and no debugger attached. The host moved Sword 3 from `(2,2)` to `(1,2)`; the peer moved Sword 4 from `(7,2)` through `(5,2)` to `(4,2)` and attacked Mortar 34 at `(3,2)`. Both native displays showed **47 HP** for the mortar, and the durable event log accepted the moves, Wait and Attack. Fog obscured distant units, so this run does not repeat the earlier full-grid memory comparison. Native Options → Surrender ended the match automatically: the host displayed Mission successful and the peer Mission failed. Both Battle info pages named `adgjmptw` as winner and showed **19:30 / 19:30** and **12 / 12 turns**. The database recorded reason `surrender`, winning team 0, and matching win/loss outcomes.

Ashen 1.0.6 was restored byte-for-byte from the supplied package, including its shared DLLs. On the same final Release build it logged in with the shared account, uploaded all nine statistics, read the Game Total leaderboard and logged in again after leaving Arena. No game executable or DLL patch was needed. The Ashen and Tomb Raider logs contained no guest panic, access violation or graphics halt.

Qt and all three Android ABI builds passed; the core test process completed with 25,589 assertions in 256 cases. All 118 service tests passed. The final Release simulator regression completed **12/12**, with Final Battle, Calculator and N95 Calculator screenshots inspected and no guest crash reported. The unchanged input implementation retains its earlier Angry Birds **5/5** result. The tested source is fork commit `56c3a2afe`. Evidence is retained locally under `data/verification/legacy-ap-2026-09-14/`, `data/verification/tomb-retest-2026-09-14/`, `data/verification/highseize/normal-2026-09-14/` and `data/verification/ashen-final-2026-09-14/`.

The reviewed emulator changes are submitted in [upstream PR #709](https://github.com/EKA2L1/EKA2L1/pull/709), source `eee48639a`. Its selected implementation and patch binaries match the tested fork; unrelated fork JPEG dispatch/plugin changes are excluded. [Upstream CI run 34783737118](https://github.com/EKA2L1/EKA2L1/actions/runs/34783737118) passed Android, iOS, macOS, Linux, Windows, dyncom differential tests, sanitizers and the aggregate CI check. Release publication was skipped for the pull request.

## Hooked positive score uploads — 14 September 2026

The final Release simulator executable above was reused on 5320 `rm-409`. The original Hooked game was entered through the online N-Gage Launcher and the normal `arena.runtime` service, using shared account `adgjmptw`. No debugger or runtime wrapper was attached. The user authorized local save editing; the complete ACE save was backed up before generating synthetic positive statistics and pending journal events. Game code and Arena leaderboard caches were unchanged.

| Native upload and returned board | Observed result |
| --- | --- |
| Total XP | `TOTAL_XP=321`, native rank 1 with 321 XP |
| Total weight | `TOTAL_MASS=1280`, native rank 1 with 5.0 kg |
| Total items | `TOTAL_ITEMS=3`, native rank 1 with 3 |
| Barracuda | `FISH_ID=3885652021`, `FISH_WEIGHT=640`, native rank 1 with 2.5 kg |
| The Hooked On Costa Rica Classic | `TOURNAMENT_ID=3164354257`, `TOURNAMENT_SCORE=987`, native rank 1 with 987 |

The first three positive reports persisted as records 19–21. A repeated native Update produced identical snapshots without increasing any board value. After restarting both the service and emulator, anonymous HTTP queries returned the same three totals without a cookie, and the Launcher logged in again. The game then loaded two synthetic unsent journal events and completed five native uploads (records 25–29), followed by its rankings page. A second Update sent only the three totals (30–32), demonstrating event acknowledgement. Normal Exit Game returned to Launcher and wrote both journal `AREN` flags as zero. Launching Hooked again through Launcher and updating sent only the three totals (33–35), with 321 XP still displayed. A final public HTTP check returned all five persisted personal bests.

A second simulator, running the same Release executable with an unedited Hooked save, logged in through Launcher as `wtpmjgda`. Its three zero-valued reports (36–38) belonged to account 2. Its native boards then displayed account 1's 321 XP, 5.0 kg total weight, 3 items, 2.5 kg Barracuda and 987 tournament points; its own totals remained zero. This independently verifies account separation and retrieval through another native client. Before login, stale NAF login data and a writable CommsDat database left by earlier diagnostics were backed up and reset. The original ROM database restored 823 entries with no persisted IAP; the transient Host network access point allowed login. No ROM or emulator code change was needed for this recovery.

The targeted ranking suite passed 13 tests, and the complete service suite finished **119/119**. The added contract case covers positive values in every category, journal-event zeros, repeated total snapshots, separate fish filters, anonymous reads and database reopening. Both native logs contained no guest panic, access violation or graphics halt. Evidence, original saves, modified fixtures, persisted reports and board screenshots are under `data/verification/hooked-positive-2026-09-14/`; the fish-board video also captures the current-player highlight animation. This validates the upload/confirmation/persistence/leaderboard flow with edited saves; natural fishing and native friends are outside this check.

## Launcher friends and shared points — 14 September 2026

Two original Launchers on the same final Release simulator executable used shared accounts `adgjmptw` and `wtpmjgda`. The sender's native Add Friend form submitted an invitation while the recipient was offline. After login, the recipient opened New Messages, displayed the original invitation text and chose Accept. Its native reciprocal subscription carried status `1`; the original sender automatically accepted that request after logging in again. Both directed relationships persisted as accepted, and both Launchers displayed the other account in Friends. The recipient also displayed the native notification that `adgjmptw` was now available to play. No friendship was inserted into the database by a fixture or debugger.

Native online chat was then confirmed in both directions: each original Launcher displayed the other account's body. For offline delivery, the sender queued `Offline paced 0012` while the recipient was stopped. The recipient's first local-server attempts fetched and purged correctly formed messages without displaying them. Outgoing XMPP capture and binary inspection showed that the Launcher pipelines roster and mailbox requests, while `nafimpssrvr_v3.dll` drops a message if PlayServer has not yet created its friend object. The loopback server was responding before that callback completed. Delaying a nonempty fetch by one second let the original client store the body, changed Home from one built-in message to two, and displayed `wtpmjgda: "Offline paced 0012"` in New Messages. The matching server row was removed by the native purge. `native-offline-message.jpg` and the guest database provide the visual and persisted evidence.

Offline messages now carry unique second-resolution UTC `jabber:x:delay` stamps; header nodes use the same value and malformed selectors are rejected. Client-supplied delay elements are replaced. Fetches can repeat until purge, and purge removes only that connection's fetched batch, preserving later or unrelated messages. Automated coverage includes same-second uniqueness, literal legacy XML element names, pacing, database reopening and malformed nodes.

Friend mini-profiles initially returned zero points from client-uploaded game history even though the achievement and global point boards held 30. The shared profile read now uses the same earned-point ledger. Native game history supplies only the application UID, so the registered game's `app_uid` maps it to the achievement class. The original friend card now displays account 1 with **30 N-Gage Points**; authenticated friend-profile and anonymous global-board probes return the same **0 solo / 30 multiplayer / 0 community** values.

All **179 service tests** completed successfully with the final code. Added coverage includes directional grants, offline invitations, reciprocal acceptance, removal and rejection, presence revocation, blocked accounts, shared game-domain identities, sender authentication, selective and repeated offline fetches, delayed purge from another resource, roster-callback pacing, database reopening, and profile/board point consistency. Temporary outgoing packet tracing was removed. There are no emulator changes in this step, so the recorded Release, Qt and Android checks remain applicable. Evidence is under `data/verification/friends-2026-09-14/`.

Native removal and rejection remain pending. XcodeBuildMCP and AXE completed login, password entry, friend-point inspection, bidirectional chat and offline-message display while macOS was locked. Mac unlock is not a prerequisite for iOS Simulator interaction.

## High Seize tentative movement review — 14 September 2026

Static analysis of the supplied unmodified `6r36.app` (SHA-256 `f2bfddf428e320ab33f2e2da89fe2d2ee25766bf8fa7dd8db6b73fb457b6b613`) exposed premature forwarding of tentative movement and a stale pending move after turn expiry. The service now holds tentative moves until the committing action, drops cancelled previews, and clears them when the turn or match ends. A flag-0 move remains immediate. This changes the service's battle path; the earlier native movement/HP pass does not validate this new ordering.

The focused transport/battle suite passed 18 tests and the complete service suite passed **144/144**. New cases cover Undo followed by reselection, timeout followed by a valid move on the next turn, manual End turn, immediate movement, surrender while a preview is pending, departure, shutdown and recovery. Native preview/cancel/timeout checks and a committed move/attack/finish control are pending. The same analysis confirmed that normal victory completion and fog-path exceptions depend on authoritative world state that the current service does not yet implement. Binary ranges, test logs and source hashes are recorded under `data/verification/highseize/preview-2026-09-14/`.

## High Seize content reader — 14 September 2026

The supplied original ZIP was read without extracting or modifying its contents. Its `data.pak` SHA-256 is `0796f98bcde127c02453a77f60a5f99c58662ecf15cff7c472f405df4dccd374`. All 1,714 catalogue names matched the native path hashes, and every resource decompressed to its declared length. All 66 NDL maps, including 34 multiplayer maps, passed the recovered grid contract. MPS1 is 9 by 11 cells; its ten encoded units include Sword 3 at `(2,2)`, Sword 4 at `(7,2)` and Mortar 34 at `(3,2)`, matching the earlier native combat checkpoints. The map also contains three properties. Loaded units are created later by the original game and are not included in these ten placements.

The standalone reader preserves non-grid sections for further analysis and does not change the live service. Its ten synthetic tests cover native hash constants and normalization, reordered catalogue entries, both compression modes, corrupt bounds/overlaps, trailing compressed data, row-major placement fields, direction bits, malformed grids and unsupported versions. The complete service suite passed **154/154** and exited successfully. Complete resource/map inspection is recorded in `data/verification/highseize/preview-2026-09-14/content-verified.json`. This is static content validation; it does not establish normal victory detection, battle CRCs or the remaining native interaction checks.

## High Seize base combat calculation — 14 September 2026

The owned 1.0.2 package now supplies all 21 unit definitions and both 21-by-21 combat matrices through `highseize_content --rules`. The loader uses the binary's internal-name registry, not the repeated and differently ordered editor identification values. The base combat calculator reproduces the game's digit-by-digit binary32 decimal parsing, Q8 strengths, single-precision damage rounding, ammunition subtraction, counterattack eligibility and use of remaining HP, and unit-loss costs.

An isolated Unicorn diagnostic executed the original ARM decimal, strength and exchange functions from code SHA-256 `37dd956090936fbce733ef99458f330c3914c009a980b253aaac526096c40b9f`. Only EUSER floating-point imports, attribute lookup, tile-bonus lookup and value-object teardown were supplied by the harness; commanders were null. All **80 decimal tokens** and **4,410 exchanges** matched the Python implementation exactly. The exchanges cover every pair of unit types, varied HP/ranges/bonuses/ammunition, 292 counterattacks, 1,546 rejected attacks and 1,089 deaths. The Sword-to-Mortar case independently reproduces the earlier native 100-to-47 HP checkpoint. These are arithmetic comparisons, not native UI matches or world-legality checks.

Fourteen synthetic tests cover the resource grammar, malformed tables, native name ordering, numeric conversion, damage boundaries, counterattack conditions and death/ammunition behavior. The complete service suite passed **168/168** and exited successfully; the owned-resource inspector verified all 1,714 resources, 66 maps, 21 unit definitions and 882 coefficients. The live relay remains unchanged. Commander abilities, map-derived bonuses, authoritative world updates and normal victory settlement are still pending, as are the remaining native interaction checks. Local oracle output and source hashes are retained under `data/verification/highseize/preview-2026-09-14/`.

## High Seize variable-length commanders — 14 September 2026

The original CMM01 writer and size function were executed with three synthetic commander objects, using a host-backed stream and memcpy import. Their 1,002-, 1,012- and 1,034-byte outputs all decoded correctly, including non-ASCII name bytes, signed modifiers and distinct values in every vector. The original size function matched `1001 + name_length + description_length` in every case. This establishes the wire contract independently of the Python fixture generator; it does not replace a native room-setup retest.

The live setup handler now validates the full record instead of requiring the earlier 1,028-byte sample and a NUL after the five-byte magic. Accepted changes clear readiness; malformed records preserve the previous valid state. Match creation persists the exact original commander bytes. Seven added tests cover layout, strings, fixed-point precision, malformed/repeated arrays, forwarding/readiness, persistence and idempotent migration of an existing database. The complete suite passed **175/175** with exit code zero. Original ARM oracle output is retained in `data/verification/highseize/preview-2026-09-14/commander-oracle.json`. Commander modifiers are decoded but are not yet applied by the base combat calculator.

## Tomb Raider baseline

The private service was exercised through the installed Tomb Raider game, GSB Arena UI and AirPlay client on N-Gage ROM `nem-4`, UID `0x101FBF9D`, in the iOS simulator. EKA2L1 used the final Release simulator build whose source matches fork commit `efb24fc9a` (base `167e5fd5252819fe4287cc3a91b55f3186f06c10` plus the general hosts and EKA1 networking changes). Debugger probes were detached and temporary emulator diagnostics removed before the final build and regression run.

| Path | Observed result |
| --- | --- |
| Cold login and persistent identity | The original Arena account reconnects after restarting the app and reinstalling the Release build. A second local device identity has a distinct account. |
| Director's Cut | A native Caves recording was trimmed, given a changed camera angle, previewed, captioned, uploaded, stored and downloaded for native playback. |
| Practice race | An authored Caves course was calibrated against the game's replay validation and completed with a visible wireframe opponent and finish crystal. Practice earns no league points. |
| Competitive challenge | A native 33.766-second recording was challenged by a 6.126-second run. The game displayed a win and +10 points. A loss displayed -5 points without crediting the returned opponent recording as the loser's best. |
| Taunt and revenge | A nonempty taunt was submitted before opening Messages in that cold client session. The opponent received it, selected the native Revenge action after a new login, downloaded the correct course, won in 2.490 seconds and submitted another taunt. The result displayed cumulative score 5 after -5 and +10. |
| Messages after identity restoration | Restoring the original account produced the incoming “A beat your time” message and the native Revenge action. |
| Monthly trophies | Temporary completed-month scores produced Gold: Arena and Silver: A in the native hierarchy. Those temporary events were removed after inspection. Live awards use actual completed UTC months. |
| Strategy guide | A locally recorded Caves clip was imported into the native level directory, downloaded and played with pause, rewind and fast-forward. A second recording was uploaded directly through the game's Strategy guide → Caves category with caption “Guide”, appeared in that level directory, and was downloaded into the native player. |
| Offline playback | With the local server stopped and no UDP listener on port 41001, the downloaded clip rewound from its end, returned to the start, played and paused at 4.12 seconds. This proves local playback within that loaded session; reopening saved guides after restarting offline is not yet verified. |
| Cold offline access | With port 41001 still closed, a restarted game retained the downloaded 642-byte `current.i3d`. Options offered no clip browser, and Arena entered Connecting before showing directories. The attempt was cancelled and the private service restored. No offline reopening path was found. |
| Guide entry inside a level | An authored Caves room-0 `adverts.dat` record displayed the native STRATEGY icon. The left softkey opened the level's guide directory directly and downloaded a clip into the native player. The same eight-byte record was then installed through the validated setup helper with a reversible backup. |

The separate live “mentor Lara” and native offline library for reopening downloaded guides after a restart described by historical accounts remain unverified and are not claimed as restored. Official downloadable courses and walkthroughs are not included. The imported clips and reference races in this local database are demonstration content, not a complete game walkthrough.

## Automated checks

- Private project: all 24 `unittest` tests passed, including actual UDP exchanges, clip/guide uploads and downloads, loss handling, duplicate results, revenge after a fresh login, trophies, guide-region layouts and limits, and reversible setup.
- EKA2L1 `ekatests`: all 5,907 assertions in 232 test cases passed; the process completed successfully from its build directory.
- Final Release default iOS regression: 12 passes, zero failures, successful process completion. FBattle2, Calculator and N95 Calculator screenshots were inspected. No guest crash, access violation or graphics halt was reported.
- The final native Arena session log was checked after reconnecting the original account. Its existing missing optional panic-blacklist-file warning is a configuration message, not a guest panic.
- [Upstream PR #707 CI](https://github.com/EKA2L1/EKA2L1/actions/runs/34707125029) passed for source `a8f332659`, which has the same nine changed files as the tested fork source: Android, Windows/Linux/macOS desktop builds and unit tests, iOS Release device build, dyncom differential testing, ASan/UBSan, and the aggregate CI check.

The user's original IMEI, nickname cookie, nine save files and logging settings were restored after the two-account checks. The requested DNS overrides and local Arena installation remain enabled. Database rows from legitimate completed test races remain available so the service has playable demonstration content; temporary trophy fixtures do not.

Local screenshots and test logs are retained under `data/verification/`, outside source control.

## Ashen native progress

On N-Gage ROM `nem-4`, UID `0x101FD3E9`, native registration, rejected incorrect credentials, successful login and login after restarting were exercised. Send High Scores saved all nine native statistics, and Total plus chapter leaderboards displayed two local accounts. The fresh-game submissions were zero; temporary positive ranking fixtures were removed after checking order. Native disconnect and manual reconnect worked. An initial offline retry required a second attempt; automatic reconnection is not claimed.

These observations precede the subsequent High Seize networking and window changes. Ashen must be retested against the final shared emulator build. Evidence is retained locally under `data/verification/ashen/`.

## High Seize native progress

The supplied full multilingual 1.0.2 package, UID `0x101FD408`, was installed with backups on N-Gage ROM `nem-4`. The native account form reached “Registration OK / Welcome to N-Gage Arena”. A later cold session rejected an incorrect password, then accepted the registered password, completed SNAP authentication and entered the Arena main menu. This confirms the HTTP interim response, empty TCP receive, Community registration, typed `authenticateUser2` result, legacy SNAP authentication, roster and presence paths.

Game-specific operations remain unfinished. Investigation also found that empty full redraws followed by partial non-redraw blits lost the rest of the page during emulator recomposition. A fix is undergoing native verification. The current IAP database is a backed-up diagnostic SDK sample and must be replaced by reproducible access-point setup before delivery. The test service uses a separate temporary database so existing Ashen accounts remain intact.

Current development checks: 42 private-service tests pass, including native UDP aggregation, challenge/proof authentication, proof replay and expiry, XMPP credential revocation, HTTP `100-continue` and typed High Seize authentication. EKA2L1's focused internet/loader run completes with 52 assertions in eight cases. These are development results, not the final Release regression required for the expanded three-game goal. No new upstream PR has been opened for the unfinished changes.

The production `arena.snap` authentication implementation was then exercised by the original High Seize client: a fresh random challenge using the live XMPP verifier completed both UDP login stages and reached operation 71 lobby lookup. This was verified in the Release simulator build containing the unspecified-family UDP bind fix (source `582677663` plus the current shared networking/window working tree). The debugger was attached with all breakpoints disabled. Lobby and room operations still run as separate diagnostic handlers and are not yet delivered as a complete multiplayer service.

A subsequent native session completed the game handshake and entered Commander settings with the local account, nationality and skill points supplied by the diagnostic server. Ready emitted a 1,036-byte commander-setup body split across two SNAP messages. The initial single-part-only probe failed to acknowledge these messages, triggering the native network-error screen after repeated retransmissions. The production `arena.highseize` codec now reassembles the native partitions, and a fresh session using that codec advanced through Commander settings to Set teams for players, showing the current account as Host. Heartbeats and native replies continued for multiple minutes without another parser error or disconnect. This establishes one-player room setup; multiplayer admission, launch and gameplay have not yet passed. Room and handshake orchestration remain in the diagnostic probe.

After that successful team-selection session, Cancel and its confirmation returned to Loading N-Gage Arena, then the original High Seize process terminated with KERN-EXEC 3 (a read from address zero). This also reproduces after cancelling a successfully connected room, so the previously observed reentry crash is not limited to a failed handshake. The exit/reentry path remains a delivery blocker and is under investigation. The game codec tests complete successfully: 48 private-service tests in total.

The room-exit crash was traced to the missing Community `Set-Cookie` response, using a debugger stop before the null read and both framework binaries. A diagnostic response containing a `JSESSIONID` populated the native retained-cookie field; the same original client then completed Commander settings, reached Set teams, cancelled and returned normally to Choose Mode without an access violation. Production Community sessions now use random cookies, preserve them across HTTP connections, expire idle sessions, bound capacity and clear authenticated identity after failed reauthentication. The full private test run completes with 51 passing tests. A second cold session using the production random-cookie implementation completed the same path and returned to Choose Mode with zero KERN-EXEC entries and zero null-address reads in its log. This result does not establish multiplayer gameplay or final emulator regression coverage.

An isolated second iPhone 16 Pro simulator was prepared with the same Release build and supplied original High Seize package. Two native accounts completed SOAP/XMPP/SNAP concurrently, with the SDK selecting UDP ports 2000 and 2001. The peer entered Rooms and displayed the host's actual `High Seize` room after a diagnostic operation-73 response using the recovered 40-byte extended record format. The peer's Select opens an Enter Room / Refresh Rooms popup; this is not yet proof of admission or gameplay. No guest binary was changed for concurrent clients.


Two clients subsequently completed native room admission, commander exchange and team readiness. Their displayed names and host privileges matched their distinct accounts. With resolved GPRS settings and MPS1, both entered the map, and server-origin battle messages advanced both clients through six end-turn transitions. Native synchronization and end-turn fixtures are covered by the High Seize tests. The current focused High Seize run has 14 passing tests; the previous full private run had 56 passing tests before the three battle-message tests were added.

A first completed diagnostic match used the host's global End current game menu. This exits to Arena without a local defeat page. The peer initially remained in play because the probe lacked automatic departure handling. Delivering the recovered player-departure and server-termination messages produced the original Mission successful screen and two Battle info pages: `adgjmptw loses!` with seven units lost, and `wtpmjgda wins!`. The peer then returned to Choose Mode. No ranked result upload was observed in this filtered match. This verifies native interpretation of those diagnostic messages, not automatic end-to-end match settlement. Normal surrender and automatic ending are still being tested. Screenshots are retained in `/tmp/eka2l1-highseize/peer-forfeit-*.jpg`.

### Two-client combat and native surrender

A fresh MPS1 match passed the following checks on the same Release simulator build, source `582677663` plus the current shared networking/window working tree. The executable SHA-256 is `dc9dfdee9e878a70c23603b4e3b4f13d7a5e54992999e1123a0360cce0b29406`. Both clients used the supplied unmodified High Seize package and the native unlimited-turn setting. Debugger access only read guest state; no coordinate, HP or guest code writes were used.

| Check | Observed result |
| --- | --- |
| Host movement | Sword unit 3 moved `(2,2) → (1,2)` and Wait completed. Both clients retained the new position. |
| Peer movement | Sword unit 4 moved `(7,2) → (6,2) → (5,2)` and Wait completed. Both clients retained the new position and continued exchanging turns. |
| Combat | On turn 3, unit 4 moved to `(4,2)` and attacked Mortar unit 34 at `(3,2)`. Both clients calculated 47 remaining HP from 100; the attacker retained 100 HP. Both native screens displayed the mortar's 47 HP. |
| State comparison | At each movement and attack checkpoint, paired read-only snapshots matched the turn, active player and IDs, owners, coordinates and HP of all ten map-occupying units. Loaded units are not included in this grid inspection. |
| Native surrender | The peer chose Options → Surrender → Yes and sent the native kind-16 message. Automatic diagnostic server completion produced Mission successful on the host and Mission failed on the peer. |
| Battle info | Both clients named `adgjmptw` as winner and `wtpmjgda` as loser, with two turns per player, zero winner losses and seven loser losses. Gold, property and construction counters also matched. |
| Timing discrepancy | The loser used-time field was `13:32` on the host and `13:31` on the peer, against the same `13:35` total. This one-second difference remains unresolved; complete agreement of all settlement fields is not claimed. |

Both clients then exited their result pages and returned to Choose Mode. The debugger sessions were detached, and the recent two-megabyte log tails contained no KERN-EXEC, access-violation or graphics-halt matches. The one-match diagnostic battle relay was stopped after the check; the account/room probe remains running.

The successful startup forwards both participants' ID-zero synchronization messages before Begin. Battle packets use the server-delivery destination bit, and Move and its following action receive separate acceptances. Earlier trials that omitted these steps rolled back movement or failed CRC verification; those are failures, not passing coverage.

Local evidence is retained under `data/verification/highseize/combat-2026-09-13/`: paired native screenshots, sanitized battle-message records and `comparison.json` with the three state comparisons and working-diff hash. The latest full private codec/service test run completed with 59 passing tests. Room and battle orchestration still lives in a temporary diagnostic process; reliable event retries, persistent results and ranked reporting remain undelivered. These observations do not replace the final Release emulator regression for the expanded goal.


### Integrated room service and durable surrender result

The temporary room/battle probes were stopped. `arena.ashen` with `arena.snap` and `arena.highseize_server` then completed a native two-client filtered Blood Bay match on the same Release simulator executable, without a debugger attached. The clients used the original 50-second turn setting. Login, room listing/admission, commander/team exchange, participant synchronization, automatic end-turn and Options → Surrender all passed through the integrated service.

The host displayed Mission successful and the peer Mission failed. Both result sequences named `adgjmptw` as winner and `wtpmjgda` as loser, reported `1/1` turns, zero winner losses and seven loser losses. Gold, property, construction and destroyed-unit counters were zero on both clients. The host displayed winner time `1:31/1:31` and loser time `1:28/1:31`; the peer displayed `1:32/1:32` and `1:28/1:32`. Elapsed-time equality remains unresolved.

SQLite contains one completed match with winner team zero, exactly two participant outcomes and one surrender event. The journal includes both ID-zero participant synchronizations, Begin, the native end-turn request and server end-turn, surrender and server End game, in that order. The peer returned normally to Choose Mode. Host exit was left pending when the Mac locked; its final result page had already been inspected. Recent two-megabyte native log tails contained no panic, access-violation or graphics-halt matches apart from the known optional panic-blacklist-file warning.

Reliable delivery now starts at sequence zero, retries identical packets and queues events in order. A reliable no-op keeps idle lobby sessions alive; the native clients acknowledged it repeatedly. The first dedicated-game heartbeat follows attachment completion in the same reliable queue. Sending it unreliably before the queued completion had caused a native connection timeout; the corrected ordering reached commander/team setup and the completed match.

All 72 private-service tests passed with the final functional changes. Local evidence is under `data/verification/highseize/production-2026-09-13/`: sanitized match/event rows, source hashes, peer result screenshots, service log, test log and native log scan. Host result images are in the task's computer-use outputs because new simulator IO capture connections stalled; the existing Simulator window remained interactive. This pass covers integrated setup and surrender persistence. It does not replace the earlier diagnostic coordinate/HP trial, final three-game emulator regression, ranked validation or general battle-outcome detection.

### N-Gage 2.0 host TLS and shared profiles

The Release simulator executable with SHA-256 `11c4e8e855e1a6ddee729884aa23b4952d90a0262e65c945216688fc0ee5876e` was built from EKA2L1 base `58267766390744481b20e5b3c93b931a46b32bd5` plus the current networking, host TLS and input working tree. Its host TLS driver and guest `ssl.dll` plugin completed native N-Gage Launcher authentication on the 5320 ROM using TLS 1.3 and a modern RSA/SHA-256 development certificate. The private service did not use its legacy TLS option. Native SNAP authentication and presence followed, and the Launcher displayed its green online indicator.

The native Launcher completed the `getMiniProfile`, `getFriendsMiniProfiles` and `getProfile` request sequence, then uploaded four game records with actual last-played timestamps. Its next profile request included the server's returned synchronization timestamp. Changing Motto to `Fishing with friends` through the original profile editor produced an authenticated `updateProfile` and persisted that exact value in the canonical account's profile. The native password and other accounts were unchanged.

The first reverse-direction check failed: after changing the test account's server-side Motto to `Fishing with friends - synced`, restarting the Launcher produced `getProfile` with its prior synchronization timestamp, followed by an upload of the old `Fishing with friends` value. A backed-up empty-cache control then displayed the exact server value without uploading the old value. Restoring the original cache and changing the server Motto to `Fishing with friends - warm` also displayed the new value, with no subsequent upload. The original local game history was preserved by restoring that cache.

Binary analysis of `playservermp.dll` identified the distinction: its `LocalProfileChanged` flag selects local editable values while an upload is pending, and successful upload confirmation clears this flag. The first test followed offline gameplay and a pending local update; the clean and restored-cache controls establish working downloads after synchronization. The implementation follows the native whole-profile conflict policy. Evidence and the original failed trial are retained together in `profile-roundtrip.json`; this conclusion does not imply automatic merging of simultaneous edits on two clients.

Hooked On was started from the online Launcher, selected its existing ACE save and reached the Costa Rica welcome page and animated map at approximately 30 FPS. This establishes startup into the game, not functioning Arena gameplay. An earlier run did not visibly respond to movement and pause inputs; a read-only debugger check confirmed key delivery and was detached afterward. A fresh launch of the same Release executable subsequently responded to held movement, pause and pocket navigation, then entered the actual fishing scene with the rod visible. No emulator input change was made between these runs, so the earlier intermittent issue is not yet explained. This proves basic local gameplay, not Arena connectivity. The game's main menu Arena Rankings screen and its Update confirmation were reached. Accepting the confirmation with the left softkey produced the native score and achievement requests documented in PROTOCOL.md. Their 501 responses did not complete the update, and a later capture was black; the saved guest log contained no game panic or access violation. Rankings and native friend messaging remain incomplete.

All 93 private-service tests completed successfully after adding shared profiles and session-heartbeat handling; the focused profile/Community run passed 22 tests. The first native ten-minute probe exposed a mistaken session-authentication check: its GET carries no credentials. After correcting the endpoint to return a stateless empty 200 response, two consecutive native probes completed at 22:10:20 and 22:11:20 (UTC+08:00), including while Hooked On was running. For this focused check the backed-up Arena XML interval was temporarily reduced; the native implementation clamped it to 60 seconds. All six original ten-minute intervals were then restored; game-written changes to other settings were preserved. Header-shape evidence contains no credentials. This does not test session expiry after an hour without authenticated activity. The host TLS tests separately covered TLS 1.2/1.3, fragmented transport, application data, invalid certificates/hostnames, corrupted records and per-process handle ownership. These results do not replace the final Release default/input regression suites or the remaining native game checks. Evidence is under `data/verification/hooked-2026-09-13/`, including the profile schema facts, native screenshots and trace, complete test outputs, native log and `profile-native-manifest.json`.


### Hooked segmented score and achievement reports

With the same Release simulator executable, native Hooked accepted the first score-submit confirmation at 22:44:59 and sent a separate `TOTAL_MASS` report. After preventing the ranking endpoint from creating anonymous session cookies, both the six-field report and `TOTAL_MASS` were committed under account 1 at 23:01:36 (UTC+08:00). The game then submitted `TOTAL_ITEMS`, which was captured and added to the supported fields. This proves native progress through the first two confirmations; it does not yet prove a completed Arena update or functioning leaderboard.

The service now persists achievement unlocks with the game's installed point definitions and idempotent account/game/achievement keys. The native `NAFAchievement` HTTP callback checks status 200 and a body beginning with `OK`. All 104 service tests passed after adding the endpoint, including transaction atomicity for invalid batches, account isolation and duplicate reports. The profile cursor comparison was also corrected to use the six-digit microsecond precision transmitted to the client; a fixed-clock test covers a submicrosecond database timestamp. Native achievement acknowledgement and ranking retrieval remain outstanding. Test output and native/database evidence are in `data/verification/hooked-2026-09-13/`.


At 23:13:01 the native game accepted all three submit confirmations and requested a filtered `topn` board for `TOTAL_XP`. The filter list contains `LOCATION_ID=378939982`, `TOURNAMENT_ID=0`, and `FISH_ID=0`. The service now parses this nested filter list and queries personal bests with account separation, stable paging and tied ranks. The response header width follows the native filtered TopN parser at `0xf8a4` (at least eight columns, returned row width equals header width minus four). Row interpretation remains under native validation. Native achievement 35 was also committed at the 23:05 Launcher login; absence of a repeated upload on the next login will help verify client acknowledgement. Restarting the private service invalidated the active SNAP association; the still-open game did not immediately reauthenticate, and its next report correctly received 403. Native validation therefore requires logging in again after a service restart.


Public ranking reads are now separated from authenticated writes. A host-side HTTP probe against the running private service, before any SNAP login, returned 200 with account `adgjmptw`'s persisted score for the native filtered TopN request; the matching native submit returned 403. Neither response created a cookie (`anonymous-http-probe.json`). This establishes the private service's access policy, not the original Arena server's policy. All 109 service tests passed with public reads and filtered nearby-rank queries. At 23:24:58 the native client accepted the filtered TopN response and requested `proximitylist` for the same stat/filter scope with `above=1`, `below=1`, `queryid=106`; this continuation is preserved in `hooked-filtered-proximity.xml`. Nearby queries and final board rendering are being validated. The native Launcher did not resend achievement 35 on the subsequent 23:21 login, consistent with accepting its earlier acknowledgement.


At 23:33:26, Hooked completed its Arena Update: three uploads were followed by 64 score-board queries (TopN and nearby lists for XP, total weight, total items, six tournaments and 23 fish types). The game returned to its rankings page and reported achievement 36, worth 20 points; the common account now holds achievements 35 and 36 for 30 points. Its XP board displays the actual account as rank 1 with the save's score of 0. The current player's row flashes on and off; adjacent video frames confirm that an initially blank first row was this highlight animation, not a failed field parse. `hooked-total-xp-ranked.png` and `hooked-own-ranking.mp4` preserve the display. The accepted row order is player name, rank, score, trailing metadata. Tests remain on that format; an unused diagnostic reordering was reverted before another network request. Launcher NGP retrieval still returns 501, and positive catch scores, native friend workflows and final normal-runtime/regression checks remain outstanding.


### Normal runtime and native Launcher point synchronization — 2026-09-14

The reload wrapper was stopped and `python -m arena.runtime` completed the same Hooked Update at 00:01:10 UTC+08: three committed reports and 64 reads, followed by the native rankings page. The current account row displays rank 1 and the actual fresh-save score of zero. The two achievement records remained unchanged at 30 points. `normal-update-manifest.json`, `hooked-normal-update.mp4` and `hooked-normal-ranked.jpg` preserve this check. Menu inactivity had activated the game's screensaver between observation and action; a contiguous wake/navigation sequence restored correct operation. No emulator input fix or guest memory modification was made for that observation.

After adding shared global/per-game NGP boards, all 114 service tests passed. The normal entry point was restarted again, and the native Launcher logged in through host TLS, queried the global board and all four installed game boards, then displayed My N-Gage Points 30, Solo Points 0, Multiplayer Points 30 and Arena Points 0. `launcher-native-points.jpg` and `point-board-contract.json` record the display, service source hashes, unchanged Release executable hash and the two persisted categorized achievements. These values were read through the native protocol, not inserted into the guest database. An anonymous HTTP probe returned those same global and Hooked point totals without setting a cookie. Cross-game aggregation, tied ranks, pagination, account separation, idempotent reports and migration from the initial achievement schema have automated coverage. Native positive catch scores, friend workflows, game-detail ranking display and the final emulator regression remain pending.

### Hostname aliases, Launcher background and temporary access points — 2026-09-14

The Release iOS build now renders the entire native Launcher login form, including its password, save-login and account-recovery controls. The defect was lost area in overlapping window invalidation regions. Region union and subtraction are checked against an independent pixel-set oracle and the native invalidation sequence. This is a shared emulator fix, with Qt and Android builds passing.

With the diagnostic CommsDat override removed, the original 5320 ROM has 823 repository entries and no IAP records. The emulator supplies Host network and its linked service/bearer/WAP records in memory. Launcher logged in and synchronized its profile and ranking requests. The persisted repository still contained the same 823 original key/value entries; the virtual records were not written. Existing access points, record-ID collisions, full tables and repository serialization have automated coverage. The N-Gage 2.0 setup helper no longer edits access-point repositories.

The iOS hosts editor saved `LocalHost.` as `localhost`. Qt and Android editors also saved hostname targets. Qt rejected a duplicate normalized source name; Android retained its mapping after a process restart. Native TLS rejected the old certificate when the alias target was absent from its subject alternative names. A replacement leaf certificate signed by the same trusted CA and including `localhost` allowed Launcher login without changing the trust anchor. Hooked On was entered through that online Launcher, completed three score submissions and 64 ranking reads, and displayed ADGJMPTW at rank 1 with the actual fresh-save score of zero. This check combined the hostname alias with the temporary access point.

Evidence is kept locally under `data/verification/hosts-2026-09-14/` and `data/verification/host-access-point-2026-09-14/`. The current shared core checks completed with 19,796 assertions in 43 cases; the ABI/loader checks completed with 48 assertions in seven cases. After removing the obsolete access-point patch helper, all 113 private-service tests passed. No game binaries, private keys, emulator data or diagnostic logs are included in the repository.

The final Release default simulator suite completed all 12 checks, including the Final Battle dwell, 5320/N95 calculators and string catalog. The five Angry Birds checks also passed, with visual confirmation of episode selection and carousel movement. Its first attempt stalled in the system screenshot service; after a simulator restart, the same touch suite completed without a product-code change. The later TLS patch rebuild clears cached server certificates when a secure session ends. Qt and Android packaging now includes that DLL and map, verified byte-for-byte against the SDK build; Android asset merging waits for native asset staging. The changed Gradle script was built with the locally installed JDK 21 because the default JDK 26 cannot compile Gradle 8.2 scripts.

### Original Ashen 1.0.6 without game patches — 2026-09-14

The complete user-supplied multilingual 1.0.6 package replaced the older diagnostic installation, with original libraries in `E:/system/libs` and no shadowing game-local DLLs. Its executable SHA-256 is `e4faf7c1e2304e1d5c64ed8249576bf1957e4d2fb87e6182d1613e26fbde4d6c`; its original `sc_lib.dll` is `d34062e62081cd955ec3af34ea171212b9b3e40ff32ff3f90d9d1d66de15e019`. The 6,350-byte CommsDB from the installation's pre-High-Seize backup was also restored; this is a previously configured database, not a pristine ROM image. Native RGenericAgent polling exposed an emulator progress mismatch: both ArenaFoundation revisions wait for EConnectionOpen (14), as defined in the SDK, while the emulator replaced that state with RConnection's 1000. Keeping the legacy stage at 14 lets the original game reach Arena.

The final Release build logged in to shared account 1, committed all nine actual zero-valued personal bests, displayed Send complete, and showed rank 1 `adgjmptw` on both Game Total and Chapter 1. These requests use SNAP/XMPP; no port-80 listener or game HTTP-port patch was needed. A wrong password produced the native failure message and a subsequent correct login succeeded. With the service stopped, connection refusal reached the guest and the waiting screen remained cancellable with Back. After restoring the service, one attempt reported failure without new XMPP traffic; the next attempt logged in successfully without restarting the game. The native log contained no guest panic or access violation. At 02:49:24 UTC, a user-started loopback port-80 relay forwarded the original registration request to the local Community service. The unmodified client displayed Registration successful, and the shared database created its new test account. That account then logged in through the native SNAP flow. No game HTTP-port patch was used.

`arena.ashen_setup` now backs up and edits only `config.yml`. The old import, HTTP-port and SNAP callback patch code and assembly were removed. Tests cover preserving executable/DLL bytes, other games' mappings, normalized hostname targets, IPv6, reversible configuration, idempotence and rejection before mutation. All 113 service tests passed after this change. Evidence is under `data/verification/ashen-v106-2026-09-14/`. Qt, all three Android ABIs and the Release iOS simulator build passed after the Nifman correction; the focused core suite again completed 19,796 assertions in 43 cases.

The subsequent final-package check logged Launcher in with the rebuilt TLS DLL, followed by native profile and point-board synchronization. A final review also added a null/type guard before reading optional patch-map requirements; all three platform builds passed, and the expanded core/loader selection completed 19,801 assertions in 44 cases.

The temporary access-point implementation applies to CommsDat-era ROMs such as the 5320. An additional High Seize control with the older pre-High-Seize CommsDB backup displayed No valid access point defined. Its former SDK sample database therefore remains an existing validation prerequisite, not evidence of an EKA1 access-point HLE implementation. This older database path and reproducible setup remain outstanding.
